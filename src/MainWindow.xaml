<Window x:Class="template_designer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" d:DataContext="{d:DesignData ViewModel}">
  <Grid>
    <Grid.ColumnDefinitions><ColumnDefinition></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <TextBox x:Name="TemplateTextBox" Margin="10,10,10,10" Grid.Column="0" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding Path=EmailTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    <WebBrowser x:Name="PreviewWindow" Grid.Column="1" Margin="10,10,10,10" />
  </Grid>
</Window>
